{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build/generic/web"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "viewer.html" }
  ]
}
